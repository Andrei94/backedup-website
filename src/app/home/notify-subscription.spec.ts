import { NotifySubscription } from './notify-subscription';

describe('NotifySubscription', () => {
  it('should create an instance', () => {
    expect(new NotifySubscription()).toBeTruthy();
  });
});
