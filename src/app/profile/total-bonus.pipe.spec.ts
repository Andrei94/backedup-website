import { TotalBonusPipe } from './total-bonus.pipe';

describe('TotalBonusPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalBonusPipe();
    expect(pipe).toBeTruthy();
  });
});
